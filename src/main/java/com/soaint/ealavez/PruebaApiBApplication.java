package com.soaint.ealavez;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Punto de entrada del microservicio API_B (persistencia de transacciones).
 */
@SpringBootApplication
public class PruebaApiBApplication {

	public static void main(String[] args) {
		SpringApplication.run(PruebaApiBApplication.class, args);
	}
}
